[package]
name = "settings"
version = "0.1.0"
edition = "2021"

[dependencies]
home = "0.5"
serde = { version = "1.0", features = ["derive"] }
simd-json = { version = "0" }
thiserror = "1"
tokio = { version = "1", features = ["io-std", "io-util"] }
