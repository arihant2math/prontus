import { StandardButton, VerticalBox } from "std-widgets.slint";

export struct UserInfo {
    name: string
}

export component UserDialog inherits Dialog {
    title: "User";
    in-out property <UserInfo> info;

    VerticalBox {
        Text { text: info.name; }
    }
    StandardButton { kind: ok; }
}