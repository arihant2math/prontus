import { Button, HorizontalBox, ScrollView, VerticalBox } from "std-widgets.slint";

export struct Embed {
    title: string,
    link: string,
}

export struct Message {
    id: int,
    user: string,
    content: string,
    images: [image],
    embeds: [Embed]
    // TODO: Embeds
}

export component ReactionComponent inherits Rectangle  {

}

export component MessageComponent {
    in property <Message> message: { // TODO: proper loading signs
        id: 100,
        user: "test test",
        content: "test message",
        images: [],
        embeds: []
    };
    HorizontalLayout {
        padding-top: 10px;
        padding-bottom: 10px;
        VerticalLayout {
            padding-left: 10px;
            Text {
                text: message.user;
                font-size: 15px;
            }
            Text {
                text: message.content;
                font-size: 14px;
            }
            HorizontalLayout {
                for image in message.images: Image {
                }
            }
        }
    }

//    HorizontalLayout { // TODO: Button group
//        width: 250px;
//        height: 45px;
//
//        Button {
//            text: "Reply";
//        }
//        Button {
//            text: "React";
//        }
//        Button {
//            text: "Edit";
//        }
//        Button {
//            text: "Delete";
//        }
//    }

    area := TouchArea {
        width: 100%;
        height: 100%;
        x: 0px;
        y: 0px;

    }
}
